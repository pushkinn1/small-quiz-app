import styled from "styled-components";

export const TextInputStyle = styled.input`
    color: black;
    padding: 10px;
    border: none;
    border-radius: 8px;

    &:focus {
        outline: none;
    }
`